/**
 * Pure-JS MLP inference matching rl/train_bc.py / train_rl.py export format.
 * Implementation: runtime-pure.js (shared with in-page pilot).
 */
import { runtime } from './load-runtime.mjs';

export const forwardPolicy = runtime.forwardPolicy;
